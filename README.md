# Golden Dream 部落格系統

一個現代化的全端部落格平台，採用 React + NestJS 架構，提供完整的文章管理、分類系統、用戶認證等功能。

## 🚀 專案特色

- **前後端分離架構**：React 前端 + NestJS 後端
- **響應式設計**：支援桌面端和行動裝置
- **豐富的編輯功能**：使用 react-markdown-editor 編輯器
- **完整的權限管理**：基於 JWT 的用戶認證系統
- **多環境部署**：支援開發、測試、生產環境
- **Docker 容器化**：提供完整的容器化部署方案
- **Google Analytics 整合**：內建網站分析功能

## 🏗️ 技術架構

### 前端技術棧

- **React 18** - 現代化 UI 框架
- **React Router v6** - 路由管理
- **Redux Toolkit** - 狀態管理
- **Bootstrap 5** - UI 框架
- **SCSS** - CSS 預處理器
- **React markdown editor** - 富文本編輯器
- **React Hook Form** - 表單管理
- **SweetAlert2** - 美觀的彈窗提示

### 後端技術棧

- **NestJS** - Node.js 企業級框架
- **TypeScript** - 類型安全的 JavaScript
- **MySQL** - 關聯式資料庫
- **Redis** - 快取和會話存儲
- **JWT** - 身份驗證
- **Swagger** - API 文檔
- **Multer** - 文件上傳處理

## 📁 專案結構

```
golden-dream/
├── client/                 # React 前端應用
│   ├── src/
│   │   ├── components/     # 可重用組件
│   │   ├── pages/          # 頁面組件
│   │   ├── services/       # API 服務
│   │   ├── store/          # Redux 狀態管理
│   │   ├── routes/         # 路由配置
│   │   └── utils/          # 工具函數
│   └── package.json
├── server/                 # NestJS 後端應用
│   ├── src/
│   │   ├── Models/         # 資料模型和控制器
│   │   ├── Providers/      # 服務提供者
│   │   ├── Config/         # 配置檔案
│   │   ├── Global/         # 全域設定
│   │   └── Utils/          # 工具類
│   └── package.json
└── Dockerfile             # Docker 配置
```

## 🛠️ 安裝與運行

### 環境需求

- Node.js 20.x
- npm 或 yarn
- MySQL 8.0+
- Redis 6.0+

### 本地開發

1. **克隆專案**

```bash
git clone <repository-url>
cd golden-dream
```

2. **切換 Node.js 版本**

```bash
nvm use 20
```

3. **安裝依賴**

```bash
# 安裝前端依賴
cd client
npm install

# 安裝後端依賴
cd ../server
npm install
```

4. **環境配置**

```bash
# 複製環境變數範例檔案
cp server/env/LOCAL.env server/.env
cp client/.env.example client/.env.local

# 根據需要修改環境變數
```

5. **啟動開發服務器**

```bash
# 啟動後端服務器 (端口 3000)
cd server
npm run start:debug

# 啟動前端開發服務器 (端口 3001)
cd client
npm start
```

### Docker 部署

```bash
# 構建 Docker 映像
docker build --build-arg ClientBuildEnv=prod --build-arg ServerBuildEnv=prod -t golden-dream:latest .

# 運行容器
docker run -p 8080:8080 golden-dream:latest
```

## 📋 主要功能

### 前台功能

- **首頁**：文章列表展示
- **文章詳情**：完整的文章閱讀體驗
- **分類瀏覽**：按分類查看文章
- **時間軸**：文章時間線展示
- **關於頁面**：網站介紹

### 後台管理

- **文章管理**：新增、編輯、刪除文章
- **分類管理**：文章分類的增刪改查
- **用戶認證**：登入、登出、權限管理
- **文件上傳**：圖片和文件上傳功能

### 技術功能

- **響應式設計**：適配各種螢幕尺寸
- **SEO 優化**：搜尋引擎友好
- **效能優化**：代碼分割、懶加載
- **錯誤處理**：完善的錯誤處理機制

## 🔧 開發指令

### 前端指令

```bash
npm start          # 啟動開發服務器
npm run build      # 構建生產版本
npm run build:dev  # 構建開發版本
npm run build:stage # 構建測試版本
npm run build:prod # 構建生產版本
npm test           # 運行測試
```

### 後端指令

```bash
npm run start:debug  # 啟動開發模式
npm run start:dev    # 啟動開發環境
npm run start:stage  # 啟動測試環境
npm run start:prod   # 啟動生產環境
npm run build        # 構建專案
```

## 🌐 API 文檔

後端提供完整的 Swagger API 文檔，啟動服務器後可訪問：

- 開發環境：`http://localhost:3000/api`
- 生產環境：`http://your-domain/api`

## 📊 環境配置

### 環境變數說明

#### 後端環境變數

- `APP_ENV`: 應用環境 (local/dev/stage/prod)
- `DB_HOST`: 資料庫主機
- `DB_PORT`: 資料庫端口
- `DB_USERNAME`: 資料庫用戶名
- `DB_PASSWORD`: 資料庫密碼
- `DB_DATABASE`: 資料庫名稱
- `JWT_SECRET`: JWT 密鑰
- `REDIS_HOST`: Redis 主機
- `REDIS_PORT`: Redis 端口

#### 前端環境變數

- `REACT_APP_API_URL`: API 基礎 URL
- `REACT_APP_GA_ID`: Google Analytics ID

## 🤝 貢獻指南

1. Fork 本專案
2. 創建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交變更 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 開啟 Pull Request

## 📝 授權條款

本專案採用 MIT 授權條款 - 查看 [LICENSE](LICENSE) 檔案了解詳情。

## 📞 聯絡資訊

如有任何問題或建議，請透過以下方式聯絡：

- 提交 Issue
- 發送 Email

---

**Golden Dream** - 讓每個想法都能閃閃發光 ✨
